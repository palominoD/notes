# training de solucion
* verificar el wolfpack para el technician training
## escenario deploy proxy
* proxy usado para que el agente salga y usado como cache de paquetes
* proxy = maquina virtual para la salida de la maquina
* esenarios de salida en proxy
    sacar un maquin aintalando desde 0
    sacar una maquina reconfigurando de online to proxy
* puerto 3128, puerto por defecto usado por vicarius por el proxy
* vradar para escaneso autenticados escaneso dedicados
* automatizaion de carga de archivos dese el escano nmap integrado con API
> un solo agente todos los modulo

